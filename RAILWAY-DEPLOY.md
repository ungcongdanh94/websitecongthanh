# Railway Deploy — v0.6

Không cần tạo database mới.

Sau khi Railway deploy thành công:

```bash
npx prisma db push
npm run db:seed
```

Kiểm tra:

```text
/admin/products
/admin/categories
/admin/brands
/admin/quotes
```
